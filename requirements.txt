# YOLOv5 Training Optimization - Dependencies
# PyTorch and CUDA (适配 RTX 3090 GPU)
torch==1.10.0+cu113
torchvision==0.11.1+cu113

# DeepSpeed for ZeRO-3 optimization
deepspeed==0.5.10

# YOLOv5 (通过源码安装)
# git clone https://github.com/ultralytics/yolov5.git
# pip install -r yolov5/requirements.txt

# 数据处理
opencv-python>=4.5.0
Pillow>=8.3.0
numpy>=1.19.0

# 科学计算
matplotlib>=3.3.0
seaborn>=0.11.0
pandas>=1.3.0
scipy>=1.7.0

# 进度条和日志
tqdm>=4.62.0
tensorboard>=2.6.0
wandb>=0.12.0

# 工具
PyYAML>=5.4.0
tensorboardX>=2.4

# 可选：用于模型分析
thop>=0.1.1  # FLOPs计算
profile  # 性能分析

# 可选：用于混合精度训练
# Apex (可选，用于更快的混合精度训练)
# git clone https://github.com/NVIDIA/apex
# cd apex
# pip install -v --disable-pip-version-check --no-build-isolation --no-cache-dir ./

# 其他依赖
pycocotools>=2.0.0  # COCO数据集API
onnx>=1.10.0  # 模型导出
coremltools>=5.0.0  # CoreML导出（可选）
